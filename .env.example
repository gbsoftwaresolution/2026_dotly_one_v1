NODE_ENV=development
PORT=3000
HEALTH_ENDPOINT_TOKEN=
DATABASE_URL=postgresql://naveenprasath-p@localhost/dotly_one_id?host=/var/run/postgresql
# Production requires a 32+ character random value. Placeholder secrets are rejected.
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
JWT_ISSUER=dotly-backend
JWT_AUDIENCE=dotly-clients
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Dotly
WEBAUTHN_ORIGINS=http://localhost:3001
# Production requires trusted HTTPS frontend origins. Do not use wildcards or localhost there.
CORS_ORIGINS=http://localhost:3001
# Set REDIS_ENABLED=false when running without Redis on purpose.
REDIS_ENABLED=true
REDIS_URL=redis://localhost:6379
STORAGE_BUCKET=dotly-local
# Leave Mailgun values blank in development to disable verification and password reset email delivery.
# Production requires all Mailgun and frontend mail URL values together.
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAIL_FROM_EMAIL="Dotly.one <noreply@dotly.one>"
FRONTEND_VERIFICATION_URL_BASE=http://localhost:3001/verify-email
FRONTEND_PASSWORD_RESET_URL_BASE=http://localhost:3001/reset-password
SUPPORT_INBOX_ALLOWED_EMAILS=support@dotly.one
# Leave Twilio values blank to disable SMS OTP delivery. When enabled, all three values are required together.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_PHONE_NUMBER=
QR_BASE_URL=https://dotly.id/q
